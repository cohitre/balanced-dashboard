<td {{bind-attr colspan=colspan}}>
	{{#if isLoadingMore}}
		<div class="loader-container">
			<span class="loader loading"></span>
		</div>
	{{else}}
		<a class="btn secondary" {{action "buttonClicked"}}>{{label}}</a>
	{{/if}}
</td>
